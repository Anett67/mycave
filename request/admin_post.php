<?php require 'param.php'; ?>
<?php require 'connect.php'; ?>

<?php   

    // $hash = password_hash("Mopsz88", PASSWORD_DEFAULT);

    // $req = $bdd->prepare("
    //                 INSERT INTO user(identifiant, password)
    //                 VALUES (:identifiant, :password)
    //             ");

    // $req->bindValue(':identifiant', "mycave_admin", PDO::PARAM_STR);
    // $req->bindValue(':password', $hash, PDO::PARAM_STR);

    // $req->execute();
 
 ?>